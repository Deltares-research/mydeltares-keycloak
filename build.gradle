project.ext {

    Properties localProperties = new Properties()
    localProperties.load(file("./gradle-local.properties").newDataInputStream())
    GITHUB_TOKEN = localProperties.get('GITHUB_TOKEN')
    GITHUB_ACTOR = localProperties.get('GITHUB_ACTOR')
}